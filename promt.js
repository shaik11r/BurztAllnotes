const username = prompt("please enter your name");
console.log(username);
alert(`hi hello welcome user: ${username}`);
/**
 * prompt is a built in funtion that displays a dailog box
 * it has a input field for the user to enter data
 * alert will display the output inside the dailog box
 */
